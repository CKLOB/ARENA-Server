# Auth Implementation Loop

## 반복 절차
1. `AGENTS.md`, `TASK.md`, `PROGRESS.md`를 읽는다.
2. `git status --short`, `git diff`와 관련 구현·테스트를 확인한다.
3. `TASK.md`의 미완료 항목 중 의존성이 가장 적은 작업 하나를 고른다.
4. 선택한 작업, 선택 이유, 예상 검증 방법을 `PROGRESS.md`에 기록한다.
5. 관련 코드와 테스트를 먼저 조사한다.
6. 필요한 최소 범위만 구현한다.
7. 해당 작업의 직접 관련 테스트를 먼저 실행한다.
8. 성공하면 `./scripts/verify.sh`를 실행한다.
9. 실패하면 최초 원인을 로그와 코드로 확인하고 `PROGRESS.md`에 기록한다.
10. 원인을 수정한 뒤 관련 테스트와 전체 검증을 다시 실행한다.
11. 실제 검증 증거가 있을 때만 `TASK.md` 체크박스를 완료 처리한다.
12. 다음 미완료 작업으로 이동한다.
13. 모든 완료 조건이 충족되면 최종 보고서를 작성하고 종료한다.

## 실패 방지 규칙
- 같은 실패가 3회 반복되면 동일 접근을 중단한다.
- 요구사항을 축소하거나 테스트를 삭제·비활성화·약화해 통과시키지 않는다.
- OAuth, JWT, Redis 동작을 임시 하드코딩으로 대체하지 않는다.
- secret, OAuth client secret, Apple private key, JWT secret을 출력하거나 커밋하지 않는다.
- 기존 사용자 변경사항을 되돌리지 않고, 요구사항과 무관한 대규모 리팩터링을 하지 않는다.
- 전체 검증이 실패한 상태에서 완료를 선언하지 않는다.
- 외부 OAuth 서버 테스트에는 provider client mock 또는 fake를 사용한다. 실제 secret 부재로 핵심 도메인 테스트를 생략하지 않는다.
- Notion 접근이 불가능하면 문서 갱신을 완료 처리하지 않고 `PROGRESS.md`에 외부 차단으로 기록한다.

## 반복 제한
- 전체 루프는 최대 20회다.
- 한 작업의 동일 실패는 최대 3회다.
- 제한에 도달하면 현재 상태, 완료 작업, 실패 원인, 사람의 판단이 필요한 항목을 `PROGRESS.md`에 기록하고 종료한다.

## 완료 기준
- `TASK.md`의 필수 체크가 검증 증거와 함께 완료됐다.
- 새 인증 계약 테스트와 기존 회귀 테스트가 성공했다.
- `./scripts/verify.sh`가 exit code 0으로 종료했다.
- git diff에 debug code, 임시 code, 실제 secret이 없다.
- `PROGRESS.md`에 미해결 실패가 없다.
- Notion 문서 갱신이 끝났거나 명확한 외부 차단으로 기록됐다.
